export type SkillsType = {
    id: number,
    title: string,
    description: string,
    imageSrc: string
}

export type SpecializationsType = {
    id: number,
    title: string,
    description: string,
    imageSrc: string
}